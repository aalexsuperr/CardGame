#!/bin/bash
# This script is no longer needed — gradlew and gradlew.bat are already included in the project.
# Simply open the project in Android Studio and it will build automatically.
echo "gradlew is already present in the project. Open the project in Android Studio to build."
